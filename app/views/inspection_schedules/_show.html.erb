<%= render('equipment/show_summary', equipment:  inspection_schedule.equipment) %>

<%= render('show_target_yearmonth', inspection_schedule:  @inspection_schedule) %>

<%= render('show_service', inspection_schedule:  @inspection_schedule) %>

<p>
  <strong><%= t('activerecord.attributes.inspection_schedule.schedule_status_id') %>:</strong>
  <%= inspection_schedule.schedule_status.name %>
</p>

<%= render('show_candidates', inspection_schedule:  @inspection_schedule) %>

<%= render('show_confirms', inspection_schedule:  @inspection_schedule) %>

<%= render('show_yes_branch_staff', inspection_schedule:  @inspection_schedule) %>
